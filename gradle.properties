org.gradle.jvmargs=-Xmx2048m -Dfile.encoding=UTF-8
# Low-spec machine: cap parallel workers so builds don't starve the rest of the PC
org.gradle.workers.max=2
org.gradle.java.home=C\:\\Program Files\\Android\\Android Studio\\jbr
android.useAndroidX=true
android.nonTransitiveRClass=true
# Avast antivirus re-signs HTTPS traffic on this machine; trust its root CA
# via a local truststore, otherwise dependency downloads fail.
# (Rebuild the store with keytool if Avast is removed/reinstalled — see README.)
systemProp.javax.net.ssl.trustStore=C\:\\Users\\mutal\\pixel-routines\\build-truststore.jks
systemProp.javax.net.ssl.trustStorePassword=routines
systemProp.javax.net.ssl.trustStoreType=JKS
